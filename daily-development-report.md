# Daily Development Report

## 2026-09-10 — 행 수·워크로드별 선택 가이드로 리포트 개편

- 사용자가 코드보다 인사이트를 우선하도록 방향을 바꿔, 본문의 pandas/SQL 발췌를 제거하고 5개 행 수 × 4개 작업의 추천 표를 추가했다. 측정 소스 사본과 원자료는 보존한다.
- 로컬에서는 단순 가공의 pandas와 집계·조인의 DuckDB, Snowflake에서는 작은 입력의 제한적인 확대 효과·1억 행의 작업별 선택·10억 행 Large의 지연 단축을 설명했다. Astra Medium과 수치 및 추천 범위를 교차 확인했다.
- 작은 평균 차이와 큰 SD는 복수 후보로 표시한다. 데이터가 있는 위치와 결과 소비 위치를 먼저 고려하고, 이번 서버 처리 시간만으로 이동 비용·다운로드 시간·credit 효율을 판단하지 않도록 명시했다.
- 기존 수치·그림과 본문 링크를 검증하고 QA의 관련 해시를 갱신한다. 실험 재실행 없이 같은 PR의 리포트 설명을 업데이트한다.

## 2026-09-10 — 6개 경로 본 실험 사후 검수·리포트 완료

- 본 실험 `run-20260909T045918099704Z`의 실제 측정은 9월 9일 13:59:18–17:29:04 KST에 완료됐다. 10만~10억 행 5개 크기 × 4개 작업 × 6개 경로 × 30회 = 3,600회이며, 9월 10일에 사후 검수와 설명 갱신을 마쳤다.
- Astra High에 원자료·통계 대조, Astra Medium에 PNG 5개 검수를 위임했다. 주 에이전트는 입력·소스 출처 확인과 한국어 해석·최종 문서 대조를 담당했다. 측정을 다시 실행하지 않았다.
- 원시 JSONL/CSV 3,600행, 120조건·6개 묶음·각 5회·경로별 600회, 예열 전체 체크섬 720개·공통 기준 20개와 timed 출력 행 수 3,600개를 검증했다. 매 반복의 전체 체크섬 검증으로 표현하지 않았다.
- 2,400개 Snowflake query ID가 고유하고 서버 시간 구성요소 누락·조회 오류는 없다. stdlib statistics로 요약 통계 1,440개·구성요소 통계 1,440개(n 포함)를 독립 대조했다. 로컬/기준 worker 260개의 로그와 1,200회 로컬 원자료도 일치했다.
- 소스 사본 20개·원본 입력 10개·분할 파일 110개의 SHA-256과 footer 행 수·ID 범위, 계정 내용·적재 성공 기록·warehouse 설정을 확인했다. 파일 해시/footer 검사와 전체 값 재계산의 범위 차이를 기록했다.
- PNG 5개에서 4개 작업 × 6개 세로 막대와 평균/표본 SD 120쌍, 0 기준 선형 축·단위·날짜·묶음 범위·음수 하한 표식을 검수했다. 실제 음수 SD 하한은 10만 행 Small 지역 집계 한 조건이다.
- [완료 리포트](results/warehouse-sweep/run-20260909T045918099704Z/report.md)에 행 수별 해석, 10억 행 X-Small 대비 Large 시간 비율 5.11~6.46배, 작은 데이터의 제한적인 확대 효과, 큰 SD·중앙값·묶음별 역전을 추가했다. 10억 행 Large는 네 작업 모두 가장 짧았으며 모든 묶음 평균에서도 유지됐다.
- 로컬 Parquet 읽기·전체 pandas 배치 생성과 Snowflake 서버 SQL 시간을 구분했다. 원격 전체 결과 다운로드·최초 적재는 주 지표 밖이고, 비용·동시성은 미측정이다. README 최신 상태/링크와 PRD를 완료 상태로 갱신했다.
- 검증 흐름: 원시 측정/worker 로그 → 통계·검증 기록 대조 → 소스·입력 해시/적재 기록 → PNG와 본문 수치·링크 대조 → QA 및 완료 리포트. 검증 스크립트와 통계·출처·차트·본문 감사 JSON을 실행 폴더에 보존했다.
- 완료 리포트 갱신 후 10분 진행보고 자동화 `snowflake`를 앱 도구로 삭제해 반복 알림을 종료했다. 실험 결과와 리포트는 보존한다.

## 2026-09-09 — 로컬 2개·Snowflake 4개 크기의 후속 실험

- 사용자가 pandas·DuckDB는 로컬, Snowflake는 X-Small·Small·Medium·Large에서 행 수별로 비교하도록 요청했다. 10만~10억 행의 5개 크기·4개 작업·6개 경로·각 30회, 총 3,600회로 구성했다. 이전 10억 행 처리 방식 대기는 이 설계로 해소됐다.
- `main.py warehouse-sweep` 진입점과 준비·로컬 배치 처리·Snowflake 측정·검증/세로 막대/리포트 모듈을 추가했다. pandas 부분 집계, DuckDB Parquet SQL, 결과 배치 순차 소비를 모든 크기에 적용한다.
- 로컬 Parquet 읽기·결과 배치 생성과 원격 서버 전체 쿼리 시간을 주 지표로 정의했다. Mac으로 원격 전체 결과를 내려받지 않는다. DuckDB 연결 생성/종료·예열·체크섬·최초 적재는 측정 밖이다. 동일 CPU/RAM·동일 출력 형식의 순수 엔진 비교로 표현하지 않는다.
- 전용 warehouse 4개 생성 및 실제 크기·Standard Gen2·단일 클러스터·자동 중지 60초를 확인했다. 키체인 인증을 재사용하고 완료한 경로의 warehouse를 중지한다.
- 최종 전체 테스트 101개와 ruff lint·새 모듈 포맷 검사를 통과했다. 빈 결과·정수 정밀도·배치/행 순서 불변성·Snowflake 플랫폼 시간 선택·조건/반복/체크섬/측정 범위 오류 차단을 확인했다. 기존 main.py와 인증 코드의 이번 변경과 무관한 포맷은 보존했다.
- 첫 파일럿은 체크섬 SQL의 예약어 `rows` 별칭 때문에 측정 0회에서 실패했다. 기록을 보존하고 `output_rows`로 고쳤다. 재실행 `pilot-20260909T044933710556Z`는 13:51:28 KST에 144회·72개 조건·72개 전체 결과 검증·96개 고유 원격 측정 쿼리로 완료됐으며 서버 지표 누락/조회 오류는 없다.
- 파일럿 PNG 3개의 6개 세로 막대·평균/표본 SD·0 기준 축·작업별 다른 범위·음수 하한 잘림 표식을 검수했다. 파일럿은 기능 검증이며 성능 결론을 확정하지 않는다.
- 원본을 보존한 적재용 분할 파일을 1억 행 10개(1,908,409,243 bytes), 10억 행 100개(19,084,212,504 bytes)로 준비했다. 전체 row group의 행 수와 연속 ID 범위를 확인했다.
- 13:59:18 KST에 `run-20260909T045918099704Z` 본 실험을 `caffeinate -i uv run --no-sync python main.py warehouse-sweep`로 시작했다. 현재 공통 검증·적재를 포함한 준비 단계이며 이후 3,600회를 측정한다. 기존 자동화 `snowflake`를 새 대상의 완료/실패 확인·5개 PNG 검수·리포트 갱신으로 수정해 활성화했다. 정상 진행 중에는 알리지 않는다.
- 흐름: 공통 Parquet → 로컬 pandas/DuckDB 결과 배치 또는 사전 COPY → 네 Snowflake warehouse의 서버 결과 → 크기별 6개 평균·SD. 예열에서 전체 체크섬, 매 반복에서 출력 행 수를 검증하며 두 범위를 구분한다.

## 2026-09-09 — Small 본 실험 1,080회 완료·리포트 검수

- `run-20260909T020641284282Z`가 11:38:56 KST에 완료됐다. 준비 포함 시작은 11:06:41이며 최대 크기는 1,000만 행이다. 10억 행 요청의 측정 결과와 구분한다.
- 36개 조건 × 30회 = 1,080회, 엔진별 360회, 조건별 6개 묶음, 216개 CALL, 1,260개 고유 child query를 확인했다. CSV와 JSONL, 모든 결과 fingerprint·출력 행 수·시간 합계가 일치하고 서버 지표 누락·조회 오류는 없었다.
- 표준 라이브러리 statistics로 평균·표본 SD·중앙값·극값·구성요소 평균을 독립 재계산했다. 저장 CSV와 차이는 0이었다. 소스 사본 해시 14개와 입력 파일 해시 6개도 일치했다. 재현용 `verify_results.py`와 근거 `qa.json`을 보존한다.
- 크기별 세로 막대 PNG 3개의 숫자·단위·0 기준 축·평균/SD·엔진 순서를 검수했다. 모두 SD 하한이 양수여서 잘림 표식이 필요하지 않았다.
- 1,000만 행 지역 집계의 SQL 평균은 141.80ms, DuckDB는 1,561.20ms였다. 조인 후 집계는 각각 215.49ms, 2,299.71ms다. DuckDB 입력 읽기·형 변환이 전체 평균의 96.4%·93.7%임을 시간 분해 표에 반영했다.
- 단순 가공의 작은 평균 차이와 묶음별 역전, 10만 행 pandas 필터의 큰 변동성을 설명했다. Python 버전 문자열의 파이프 문자로 Markdown 표 열이 갈라지는 표시 문제와 누락 0개 설명 문구를 고쳤다. 측정 코드는 바꾸거나 재실행하지 않았다.
- README·PRD·이전 로컬/X-Small 리포트의 후속 실험 링크를 완료 결과로 갱신했다. 10억 행 입력은 준비 완료, 처리 방식은 선택 대기 상태로 유지한다.
- 최종 표의 평균/SD 36쌍과 시간 분해 24쌍, 내부 링크·Markdown 표 열 수를 확인했다. 완료 알림 자동화 `snowflake`를 PAUSED로 바꾸어 반복 완료 알림을 중지했다.

## 2026-09-09 — 10억 행 후속 실험 준비

- 사용자가 Snowflake 테이블을 10억 행으로 늘리도록 요청했다. 현재 Small 본 실험의 정상 진행을 확인하고 공통 seed 20260907의 10억 행 Parquet 준비를 시작했다. 데이터 생성은 50만 행 단위이며 준비 상태·완료·실패는 `results/snowflake-small/billion-preparation.json`에 남긴다.
- 여유 디스크 422,976,720,896 bytes 확인. 전체 입력/결과를 메모리에 보유하는 기존 방식의 최소 데이터 크기와 Python 런타임의 관측된 12GiB를 비교했다.
- Snowflake 공식 `to_pandas_batches` 문서와 Python 프로시저 메모리 제약, 이전 4개 DB 실험의 결과 크기 1~1,000행을 확인했다. 배치 처리와 전체 DataFrame 방식은 측정 의미가 달라 사용자에게 선택을 요청했다. 원격 10억 행 측정은 방식 확정 후 수행한다.
- 공통 원본 준비 완료: Parquet 19,083,998,521 bytes(약 19.08GB), 10억 행·8개 컬럼·10,000개 row group. footer의 행 수와 각 row group의 연속 ID 범위, 계정 10만 행을 확인했고 생성 시 원본 SHA-256을 보존했다. 이 데이터는 아직 Snowflake에 적재하거나 반복 측정하지 않았다.

## 2026-09-09 — 세로 막대 전환과 Small 내부 실험

- 사용자 요청으로 기존 1,800회 결과의 크기별 그림 3개를 세로 막대로 변경했다. 평균·표본 SD 60쌍 대조 및 모든 PNG의 숫자·축·하한 잘림 표시를 확인했다.
- 기존 측정 소스를 기록된 SHA-256과 대조하고 해당 실행 폴더의 `source/measured/`에 보존했다. 사후 검증기는 이 사본을 사용하므로 후속 구현과 기존 측정의 출처가 섞이지 않는다.
- 전용 `FRAME_BENCH_SMALL`을 생성했다(Small, 단일 클러스터, 자동 중지 60초). Python 임시 프로시저에서 pandas 2.3.3·DuckDB 1.5.5가 실행됨을 확인했다. 처음 pyarrow 미포함 probe는 실패했고 패키지를 명시한 probe는 성공했다.
- `warehouse_benchmark.py`는 적재·무작위 반복·저장, `warehouse_handler.py`는 서버 내부 측정, `warehouse_report.py`는 전수 검증·통계·세로 막대·리포트를 담당한다. main.py의 새 `warehouse-frames` 명령으로 실행한다.
- 흐름: 공통 Snowflake 테이블 → Python pandas / Python DuckDB / native SQL → 서버 내부 전체 DataFrame → 시간 종료 → fingerprint 검증 → Mac에 지표 반환. 원격 프로시저 시작·Mac 다운로드 시간은 주 측정에서 제외한다.
- 오프라인 검사 69개 및 ruff 통과. 빈 입력을 포함한 3개 경로의 결과, 프로시저 소스 구성, 누락·중복·결과 불일치·시간 합계·묶음 오류 차단을 검증했다.
- 실제 Small 파일럿 `pilot-20260909T020311347173Z`는 72회·모든 fingerprint·서버 query history 84개 확보·평균/SD 독립 대조를 통과했다. 이어서 `run-20260909T020641284282Z`에서 1,080회 본 실험을 시작했다. 두 실행 모두 키체인을 비대화형으로 재사용했다.
- 파일럿 세로 막대 PNG 3개 표시 검수를 완료했다. 기존 완료 확인 자동화 `snowflake`의 대상을 새 Small 본 실험으로 갱신했다. 정상 진행 중에는 알리지 않고 완료·실패 시 검증·리포트 갱신 후 알리도록 설정했다.

## 2026-09-09 — 이전 형식의 평균 ± SD 막대그래프 적용

- 이전 실험의 한국어 가로 막대·정확한 평균 ± 표본 SD 표기를 이번 세 엔진 결과에 적용했다. 입력 크기별 PNG 3개에 4개 작업·5개 실행 경로를 표시한다.
- `source/mean-sd/main.py`에서 원자료와 평균·SD 60쌍을 대조한 뒤 렌더링했다. 0 기준 선형 축, 엔진별 무늬, 로컬·원격 구분, 음수 SD 하한의 잘림 표식을 직접 확인했다.
- 리포트에 세 크기의 그림과 설명을 추가하고 기존 표·그래프는 보존했다. PRD와 시각화 검증 기록을 갱신했다.
- Snowflake 공식 문서에 근거해 같은 Small에서의 후속 실험이 통제하는 조건과 Python 저장 프로시저·SQL 자원의 차이를 설명했다. 일반 pandas와 하이브리드 실행을 쓰는 pandas on Snowflake를 구분했다. DuckDB의 계정 내 런타임 호환성은 아직 검증하지 않았다.
- 보고 흐름: 기존 1,800회 원자료 → 평균·표본 SD 60쌍 대조 → 크기별 가로 막대 PNG → 리포트 반영. 원격 실험·warehouse 변경은 수행하지 않았다.

## 2026-09-09 — 세 엔진 본 실험 완료와 리포트 업데이트

- 사용자 터미널에서 키체인 인증으로 실행한 `run-20260909T004556934215Z`가 09:57:55 KST에 완료됐다. 60개 조합 × 30회 = 1,800회이며 pandas·DuckDB 각 720회, Snowflake 360회다.
- 원시 JSONL·CSV, 조합·반복·묶음 누락 및 중복을 확인했다. Python 표준 라이브러리의 평균·표본 SD·중앙값·극값 재계산이 summary와 일치했다. 로컬 배율 24개와 Snowflake 구성요소 통계 576개도 대조했다.
- 측정 소스·입력 파일 해시를 확인하고 원본 Parquet에서 새로 계산한 pandas의 전체 결과 12개가 저장된 기준 fingerprint와 일치함을 확인했다. 원격 결과 본문은 보존되지 않아 당시 검증 성공 기록과 측정 코드를 근거로 범위를 명시했다.
- Snowflake 고유 query ID 360개에 서버 지표 누락·조회 오류가 없었다. 측정된 provisioning·overload queue는 모두 0ms였다.
- 기존 자동 생성 리포트의 표·그래프·측정 조건을 유지하면서 세 엔진 핵심 결과를 앞에 배치하고, 1,000만 행 시간 구성요소의 평균 ± SD, 1회 업로드·적재 시간, 큰 SD·묶음별 역전·시스템 swap 활동의 해석을 추가했다.
- 기존 그래프 4개의 축·범례·오차막대·로그축 하한 잘림 표시를 직접 확인했다. README의 최신 결과와 완료 상태, PRD를 갱신했다. 기존 실험과 10억 행 DB 리포트는 수정하지 않았다.
- 검증 절차와 결과는 해당 결과 폴더의 `verify_results.py`, `qa.json`에 보존한다. 후속 리포트 작업에서 벤치마크나 원격 쿼리를 다시 실행하지 않았다.

## 2026-09-09 — macOS 키체인 인증 추가

- 사용자가 반복 비밀번호 입력을 없애기 위해 macOS 키체인 저장을 선택했다.
- `--snowflake-keychain`은 프로필의 account·user에 해당하는 비밀번호를 조회한다. 최초에는 터미널 비표시 입력 후 Snowflake 인증 성공 시 저장하고 곧바로 실험을 수행한다.
- 등록된 항목은 비대화형 실행에서 자동으로 사용한다. `--snowflake-password-prompt`를 함께 지정하면 인증에 성공한 새 비밀번호로 갱신한다.
- 키체인 접근은 네이티브 macOS backend로 한정했다. 비밀번호를 프로필·CLI 인자·manifest에 추가하지 않으며, 실패한 인증으로 기존 저장 항목을 변경하지 않는다.
- 인증·키체인 등록 중 Ctrl+C도 실패 상태로 기록하고 열린 연결을 정리한다.
- keyring 25.7.0 및 필요한 의존성을 uv로 설치했다. pandas 2.3.3, DuckDB 1.5.5, Snowflake Connector 4.7.3은 그대로다.
- 실제 macOS 키체인의 임시 테스트 항목으로 저장·조회·삭제를 확인하고 테스트 항목을 제거했다. 구현 직후에는 실제 Snowflake 비밀번호 등록 전이었고, 이후 사용자 터미널에서 등록·실험을 완료했다.
- 검증: 기존·추가 테스트 39개 통과(전체 38개 확인 후 취소 처리 보강에 대해 키체인 테스트 11개 재검증), ruff와 diff 검사 통과. 비대화형 재사용, 계정·사용자 분리, 인증 후 저장, 실패한 비밀번호 교체 보존, 빈 입력 거부, 등록 실패·취소 시 연결 종료와 실행 상태를 확인했다.
- README·PRD에 최초 등록 겸 본 실험 명령, 비밀번호 갱신 방법과 인증 흐름을 반영했다. 구현 직후 최초 입력 대기 상태였으며, 후속 완료 내역은 위에 기록했다.

## 2026-09-09 — 파일럿 완료 및 본 실험 시작 시도

- 사용자 터미널에서 실행한 `pilot-20260908T235954570516Z`의 완료 manifest를 확인했다. 세 엔진 60개 조건에서 각 2회, 총 120회이며 Snowflake query history 조회 오류는 없었다.
- 본 실험 명령을 10만·100만·1,000만 행, 각 조건 30회로 실행했으나 저장되지 않은 비밀번호 입력에서 대기했다.
- 도구 실행 PTY가 사용자에게 표시되는 앱 터미널에 연결되지 않아 인증 전에 해당 프로세스를 종료했다. 생성된 본 실험 manifest는 0회·실패 및 중단 이유로 정리했다.
- 본 실험의 원격 업로드나 시간 측정은 아직 없다. 사용자 터미널에서 비밀번호를 다시 입력해 실행해야 한다.

## 2026-09-09 — Snowflake 계정 확인 및 터미널 인증 준비

- 사용자 로그인 후 Chrome에서 계정과 기존 COMPUTE_WH(X-Small), 개인 데이터베이스의 PUBLIC 스키마를 확인했다.
- 기본 externalbrowser 연결은 SAML IdP 오류 390190으로 실패했다. 콘솔 로그인 경로도 웹 홈으로 이동했지만 Python 인증 콜백은 완료되지 않아 시도를 종료했다.
- 실제 식별자는 저장소 밖 로컬 커넥터 프로필에만 저장했다. 비밀번호·토큰은 저장하지 않았다.
- `--snowflake-password-prompt`를 추가해 대화형 터미널의 비표시 입력으로 인증하도록 했다. 연결 이름 필수, 비대화형 입력 차단, 인증 캐시 미요청을 적용했다.
- 검증: 기존·추가 테스트 27개 및 ruff 통과. 새 테스트는 CLI 필수 조건, 비대화형 입력 차단, 연결 인자 전달과 출력 내 비밀번호 부재를 확인한다.
- 인증이 완료되지 않아 업로드·원격 쿼리 측정은 아직 시작하지 않았다. 사용자 입력을 받아 파일럿부터 실행할 준비 상태다.

## 2026-09-08 — Snowflake 비교 경로 추가

- 요청: 기존 pandas vs DuckDB 전처리 비교에 Snowflake 추가.
- 연결 도구·환경변수 이름·커넥터 기본 설정 파일 경로를 확인했으나 사용 가능한 연결은 없었다. 계정·실험용 warehouse 연결을 사용자에게 요청했다.
- 기존 `main.py frames`에 선택적 `--snowflake-connection` 추가. 기본 로컬 실행과 기존 결과는 보존하고 3개 엔진 사용 시 공통 환경에서 새로 측정한다.
- 공통 Parquet를 원격 임시 stage/table로 적재, 결과 재사용 캐시 비활성화, SQL부터 fetch_pandas_all 완료까지 측정, 전체 결과 교차 검증, query ID별 서버 메타데이터 수집을 구현했다.
- 원격 warehouse와 로컬 parquet/memory의 입력 조건을 구분하고 업로드·적재 시간은 별도 보존한다. 서버 메모리를 로컬 RSS로 표시하지 않는다.
- Snowflake Connector 4.7.3의 pandas 호환 요구로 lockfile 공통 pandas가 3.0.5에서 2.3.3으로 변경됐다. 기존 수치 대신 새 공통 환경에서 재측정하도록 명시했다.
- 실제 인증정보가 없는 브라우저 SSO 연결 예시 추가. warehouse는 기존 설정을 사용하며 생성·확대·강제 중지는 하지 않는다.
- 원격 실측은 미실행. 테스트의 모의 응답은 임시 테스트 폴더에서만 사용하며 Snowflake 실험 결과로 취급하지 않는다.
- 검증: 기존·추가 오프라인 테스트 총 24개 통과. DuckDB로 이식 가능한 SQL 계산 의미를 확인했으며 이는 Snowflake 서버 검증을 대체하지 않는다.
  모의 원격 경로의 측정 행렬·실패 상태·세션 종료, 타입 정규화·빈 입력·정수 정밀도를 확인했다.
- 표시 검증: 합성 값임을 명시한 임시 그래프에서 3개 엔진 표식·단위·오차막대를 확인했다.
  로그축에서 SD 하한이 0 이하인 경우의 잘림 표식을 추가했다. 서버 지표가 모두 누락된 경우에는 count=0·빈 통계를 유지하며 경고 없이 출력되는지 확인했다.
- Ruff·CLI 도움말·diff 공백 검사 통과. README·PRD에 연결 방법, 실험 조건, 데이터 흐름과 현재 미실행 상태를 갱신했다.

## 2026-09-07

- 사용자 요청으로 `results/linkedin-post.md`를 삭제했다. 보고서의 해당 파일 링크와 산출물 흐름을 정리하고, PRD를 게시글 전문은 채팅에만 제공하는 방식으로 변경했다. 게시글은 baseline·optimized와 세 집계 쿼리의 결과를 구분해 다시 작성한다.

- GitHub에서 보고서 이미지가 깨지는 원인인 Mac 절대 경로를 수정했다. `benchmark-report.md`의 이미지 4개와 문서·원시 결과 링크 17개를 보고서 기준 상대 경로로 변경하고, PRD에 저장소 문서의 링크 규칙을 추가했다.
- 검증: 내부 링크 21개 모두 Git에 포함된 파일로 연결되며 이미지 4개의 PNG 형식을 확인했다. 링크 주소를 제외한 보고서 내용은 동일하고 `git diff --check`를 통과했다.

- LinkedIn 게시글의 문체를 실험 경험을 공유하는 자연스러운 글로 수정했다. DuckDB에 관심이 생긴 이유를 도입에 담고, 5개 불렛·핵심 수치·실험 범위를 유지하면서 보고서식 표현을 줄였다.

- 보고서를 DB 선택 판단 → baseline·optimized 비교 → DB별 추천 워크로드 순서로 전면 재작성했다. DuckDB의 분석 전반 경쟁력, ClickHouse의 집계 성능, PostgreSQL·SQLite의 indexed lookup 강점을 구분했다.
- 최적화의 평균 변화뿐 아니라 SD·p95·저장 공간·구축 비용 범위를 함께 설명하고, 프로덕션 추천은 공식 문서에 근거한 판단으로 표시했다. 실제 SQL·검증·중단·파일럿 제외 근거는 유지했다.
- 원시 데이터로 모든 수치 표를 재계산했다. SQLite optimized의 medium p50은 69,766.308 ms, large p50은 393,562.730 ms로 기존 표를 정정했다. 나머지 표도 동일한 소수 셋째 자리 반올림을 적용했다.
- `results/linkedin-post.md`에 도입과 결론, 정확히 5개 핵심 불렛을 포함한 게시글 전문을 작성했다. 외부 게시 작업은 수행하지 않았다.
- 차트 제목의 이전 절 번호를 제거하고 캐시 조건을 ‘검증 후 반복 측정’으로 명확히 했다. 시각화 코드의 긴 문자열은 표시 내용을 유지하며 줄을 나눴다.
- 검증: 8개 validation과 930개 완료 측정의 checksum·행 수 일치, 31개 통계 조합과 15개 프로파일 쌍의 표 수치 일치, 로컬 링크 21개·차트 4개·SQL 블록 4개·게시글 불렛 5개 확인. Ruff 검사·포맷 검사 통과, 네 차트의 제목·축·범례·오차막대·미완료 표시를 렌더링으로 확인했다.
- 보고 흐름: 검증된 원시 결과 → 통계·비교·차트 → 관측과 추천 구분 → 전체 보고서와 LinkedIn 게시글. PRD의 산출물·목차·캐시 설명과 조인 관계를 함께 갱신했다.

- 6.1절에 baseline·optimized 직접 비교 차트와 평균 ± 표본 SD·속도 배수 표를 추가했다. 네 엔진의 15개 완료 쿼리 쌍을 비교하고 SQLite join은 미완료로 표시했다.
- 검증: 원시 데이터·summary 통계 대조, 비교표 16행(유효 15쌍)과 이미지 링크 확인, Ruff 통과, 차트 렌더링의 축·범례·SD 하한 표시 확인.
- 비교 흐름: 프로파일별 원시 측정 검증 → 평균·SD 계산 → 동일 엔진·쿼리끼리 결합 → 속도 배수 계산 → 비교 막대와 표 생성.

- 보고서 5·6·8절에 성능 차트와 저장 크기 비교 차트를 추가했다.
- 사용자 피드백에 따라 5·6절을 평균 응답 시간 세로 막대 차트로 변경했다. 낮을수록 빠름, 쿼리별 순위, 최속 엔진 강조를 추가하고 p50/p95는 표로 유지했다.
- 5·6절 평균 막대에 ±1 표본 표준편차 오차막대와 수치를 추가했다. 30회 원시 측정의 SD를 계산하고 완주 summary의 stddev_ms와 대조한다. 로그 축 아래로 내려가는 하한은 ▽로 구분한다.
- 시각화 구조: 원시 측정 → 평균·표본 SD 계산 및 summary 대조 → 쿼리별 평균 순위 → 평균 막대 + SD 오차막대 4개 패널 → 5·6절 PNG.
- 원시 측정의 반복 수·iteration·checksum 및 완주 summary의 평균·p50·p95를 검증한 뒤 차트를 생성한다.
- SQLite optimized의 3개 완료 쿼리는 원시 측정값을 사용하고, join은 중단으로 표시한다.
- PostgreSQL optimized 저장 필드 간 약 0.001 GiB 차이가 있어 차트는 total_bytes / 2^30으로 계산했다. 두 값 모두 소수 둘째 자리에서 212.76 GiB다.
- PostgreSQL optimized 저장 크기를 현재 10억 행 storage.json의 212.76 GiB로 보완했다.
- 재생성: `uv run scripts/visualize_results.py`. PNG는 `results/charts/`에 저장한다.
- 흐름: 결과 JSON/JSONL → 행 수·반복·checksum 검증 → 통계 계산·summary 대조 → PNG 생성 → Markdown 보고서 표시.


## 2026-09-06

### 10억 행 최종 실험 정리

- 기준선 프로파일의 ClickHouse·DuckDB·PostgreSQL·SQLite는 모두 10억 행 적재·검증·쿼리별 30회 측정을 완료했다.
- 최적화 프로파일의 ClickHouse·DuckDB·PostgreSQL도 10억 행 검증과 쿼리별 30회 측정을 완료했다.
- 이전 생성식 오류를 수정한 DuckDB optimized 결과는 네 쿼리 checksum이 기준선과 일치해 유효 처리했다.
- 최적화 SQLite는 `small`, `medium`, `large`를 각 30회 완료했다.
- 최적화 SQLite `join`은 첫 실행이 6시간 13분 동안 결과를 반환하지 않아 중단했다. 중단 시점의 CPU 사용률은 약 100%였고, 프로세스가 멈춘 것이 아니라 장시간 실행 중인 상태였다.
- 중단 직전 SQLite optimized volume 사용량은 약 107.40GiB였으며, 컨테이너와 named volume을 삭제했다.
- 최적화 SQLite의 10억 행 `summary.json`·`storage.json`·manifest는 생성되지 않았고, 기존 동일 파일은 10,000행 파일럿 결과로 남아 있다. 10억 행 최적화 SQLite `measurements.jsonl`에는 `small`, `medium`, `large` 각 30회만 보존했다.
- 모든 엔진·프로파일의 1회 검증 checksum은 일치했다. 최적화 SQLite `join`은 결과 미반환으로 checksum이 없다.
- 최종 결과 보고서를 `results/benchmark-report.md`에 갱신했다.

## 2026-09-03

### 완료

- ClickHouse, DuckDB, SQLite, PostgreSQL 비교 목표와 실험 조건을 PRD로 정리했다.
- 단일 DB 순차 실행, 1회 결과 검증 후 100회 warm-cache 측정 정책을 정의했다.
- 20개 컬럼의 결정론적 데이터 모델과 4개 쿼리 범주를 정의했다.
- Docker Compose와 Python 기반 오케스트레이터/실행기 구조를 구현했다.
- 결과 checksum, 원시 측정값, 평균·최소·최대·표준편차·p50·p95 저장을 구현했다.

### 검증 상태

- Docker CLI 및 Docker Compose CLI: 설치 확인
- Docker Desktop 기동 후 실제 컨테이너 파일럿 완료
- 10,000행 파일럿: 4개 엔진 모두 적재·검증·측정 성공
- 파일럿 100회 반복: 4개 엔진 모두 성공
- 4개 쿼리의 엔진 간 checksum 일치 확인
- 호스트 여유 공간: 약 322GiB
- 용량 기준 정정: 10억 행 1개 DB 기준 약 372.5GiB, 5억 행 1개 DB 기준 약 186.3GiB
- 5억 행의 4개 volume 보존 기준 합계는 약 745.1GiB이며, 현재 여유 공간 약 322GiB로는 자동 차단
- 요구사항 변경: 최종 목표를 5억 행으로 조정
- 요구사항 추가: 100만 행 `account_dim` dimension 테이블과 4번째 join 쿼리 추가

### 파일럿 100회 결과

단위는 ms이며, fact 10,000행과 dimension 1,000,000행의 warm-cache 기준이다.

| DB | small 평균 | medium 평균 | large 평균 | join 평균 |
|---|---:|---:|---:|---:|
| ClickHouse | 1.198 | 2.504 | 2.426 | 6.045 |
| DuckDB | 0.122 | 0.491 | 1.137 | 1.187 |
| SQLite | 0.146 | 0.374 | 2.142 | 109.507 |
| PostgreSQL | 0.306 | 0.466 | 1.741 | 93.808 |

파일럿 결과 전체는 `results/pilot-100-summary.md`에 기록했다. 이 수치는 5억 행 성능을 예측하는 결과가 아니다.

### 다음 작업

1. 조인 포함 파일럿 결과를 기준선으로 사용한다.
2. 충분한 저장공간 확보 후 5억 행/100회 본 실험을 실행한다.

### 10억 행 재실험 준비

- 요구사항 변경: fact 테이블을 10억 행으로 확대
- 반복 횟수: ClickHouse·DuckDB 100회, PostgreSQL·SQLite 10회
- volume 삭제 직전 실제 저장 크기를 `results/<engine>/storage.json`에 기록하도록 오케스트레이터를 보완했다.
- 10억 행 ClickHouse 1차 시도는 join checksum 불일치로 중단했다. 병렬 `Float64` 합산의 반복별 미세 차이를 제거하기 위해 금액·할인율 집계를 정수 센트 기반으로 변경한 뒤 재실행한다.

### 10억 행 실험 진행 상태

- ClickHouse: 10억 행 적재·검증·쿼리별 30회 측정 완료. 실제 저장 크기 66.97GiB(로그 포함), volume 삭제 완료.
- DuckDB: 10억 행 적재·검증·쿼리별 30회 측정 완료. 실제 저장 크기 9.96GiB, volume 삭제 완료.
- PostgreSQL: 10억 행 적재·검증 완료 후 쿼리별 30회 측정 진행 중. 적재 후 DB 크기는 약 178GB이며, `small` 1회가 약 1분 45초 소요되는 상태다.
- SQLite: PostgreSQL 완료 후 10억 행·쿼리별 30회로 실행 예정.

### 실험 2: optimized 프로파일

- ClickHouse optimized: 10억 행 적재·검증·쿼리별 30회 측정 완료. 실제 저장 크기 73.26GiB, volume 삭제 완료.
- SQLite optimized 파일럿: 10,000행·쿼리별 1회 적재·검증·측정 성공.
- DuckDB optimized의 3개 ART 인덱스 생성은 약 33분 후 24.6GiB 메모리 한도로 OOM 종료됐다. 해당 volume은 삭제됐다.
- DuckDB optimized의 전역 정렬은 임시공간 200GB 한도에서 약 186GiB 사용 후 중단했고, 호스트 여유 공간 보호를 위해 재시도하지 않았다.
- DuckDB optimized 전략을 event_day 그룹별 직접 순서 적재로 변경해 `(event_day, id)` 물리 레이아웃과 zone map을 활용한다. DuckDB 대형 ART 인덱스는 생성하지 않는다.
- 변경된 DuckDB optimized 파일럿 재검증 성공. 다음 단계는 DuckDB 본 실험, 이어서 PostgreSQL·SQLite optimized 실행이다.
- PostgreSQL optimized: 10억 행 적재·4개 B-tree 인덱스 생성·1회 결과 검증 완료. volume은 약 213GiB이며, 쿼리별 30회 측정 중이다.
- PostgreSQL optimized 측정은 NUMERIC 센트 집계의 안정성을 유지하는 대신 medium 반복 1회가 약 2분 수준으로 진행 중이다. 최종 summary 확정 전에는 중간값을 사용하지 않는다.
- 교차 엔진 checksum 점검에서 DuckDB optimized의 직접 순서 적재 생성식 오류를 발견했다. `id` 표현식 괄호가 없어 category/region/event 관련 값과 집계 행 수가 기준선과 달랐으며, 해당 30회 측정값은 무효 처리한다.
- 오류 수정: DuckDB 생성식의 복합 `id` 표현식을 괄호로 감싸고 schema version을 v3으로 올렸다. 수정된 DuckDB optimized 본실험을 재실행해야 한다.
- 현재까지의 유효 결과와 미완료 항목은 `results/benchmark-report.md`에 기술 리포트 형식으로 정리했다.

### 5억 행 본 실험 진행 상태

- 실행 방식: `ClickHouse → DuckDB → PostgreSQL → SQLite` 순차 실행
- ClickHouse·DuckDB: 5억 행 fact + 100만 행 dimension 생성 → 1회 검증 → 4개 쿼리 100회 측정 → volume 삭제
- PostgreSQL·SQLite 후속 실행: 같은 데이터 생성·검증 후 쿼리별 1회 측정 → volume 삭제
- ClickHouse: 적재·검증·100회 측정 완료, volume 삭제 완료
- DuckDB: 적재·검증·100회 측정 완료, volume 삭제 완료
- SQLite: 쿼리별 1회 정책으로 재실행 중. 적재 완료 후 1회 검증 진행 중
- PostgreSQL: 쿼리별 1회 측정 완료. 최초 실행에서 join 금액 checksum 차이를 발견해 NUMERIC 합산으로 수정 후 재실행했고, 4개 checksum 모두 일치

현재 본 실험에서 완료된 warm-cache 100회 평균(ms)은 다음과 같다. 전체 결과 확정 전의 부분 결과이며, checksum은 쿼리별로 ClickHouse와 DuckDB가 일치한다.

| DB | small | medium | large | join |
|---|---:|---:|---:|---:|
| ClickHouse | 373.986 | 219.259 | 237.003 | 2,316.830 |
| DuckDB | 1.748 | 180.571 | 253.770 | 312.295 |
| PostgreSQL | 54,149.819 | 60,236.738 | 102,170.229 | 181,471.067 |

SQLite 기존 측정에서 medium 단일 반복이 약 80~115초였고 100회 수행은 비현실적으로 긴 것으로 확인됐다. 따라서 SQLite는 쿼리별 1회로 고정한다. PostgreSQL도 현재는 쿼리별 1회 결과를 기준으로 보며, 100회 반복은 추가하지 않는다.
# 2026-09-07 pandas / DuckDB 전처리 비교 추가

- 사용자 요청: 작은·중간·큰 데이터의 전처리/변환을 엔진별 30회 실행하고 평균·표준편차 등 다양한 통계 비교.
- 기존 파일·DB 결과를 보존하고 `main.py frames` 경로를 추가했다.
- 구현: 공통 Parquet 입력, 4개 작업, 파일/메모리 입력 분리, `.df()` 포함 시간,
  독립 worker 프로세스, 6개 묶음 순서 무작위화, 전체 결과 fingerprint 비교.
- 통계: 평균·표본 SD·분위수·IQR·MAD·CV·처리량, 묶음 기반 평균 구간·대응 묶음 bootstrap 배율.
- 메모리: 검증 해시의 할당을 제외하도록 별도 프로세스의 첫 연산 직후 OS 최대 RSS 수집.
- uv 프로젝트와 lockfile, 경계값/빈 입력/필터 pushdown/통계 테스트, ruff 설정 추가.
- 기본 크기는 10만·100만·1,000만 행. 호스트 48GiB RAM·논리 CPU 14개에서 수행하며
  외부 CPU 부하가 관찰되어 실행 순서별 시스템 상태를 기록한다.
- 검증: 단위 테스트 12개·ruff 검사/포맷 통과, 기존 DB CLI와 새 CLI 도움말 확인.
- 파일럿: 48개 조합 × 2회 완료 및 엔진·입력 방식 간 전체 결과 fingerprint 일치.
- 본 실험: 2026-09-07 11:28–11:33 KST, 48개 조합 × 30회 = 1,440개 시간 측정과
  조합별 별도 메모리 1회 완료. 모든 결과 fingerprint 일치, 원시 JSONL에서 평균·표본 SD·중앙값·극값·비율 독립 재계산 일치.
- 관측: 메모리 입력의 결측치·파생 컬럼은 pandas가 3개 크기 모두 우위.
  1,000만 행 Parquet 지역 집계는 DuckDB 3.36배, 조인 후 집계는 7.88배 빠름.
  100만 행 메모리 필터 작업의 작은 평균 차이는 구간이 1을 포함해 우위를 단정하지 않음.
- 환경: 시스템 전체 swap 입출력과 배경 부하 관측. 합성 수치형·warm-cache·최대 논리 610MiB 범위로 결론 제한.
- 시각화: 시간 분포 2개 및 배율 1개 PNG를 직접 확인. 배율 그래프의 눈금 겹침을 수정하고
  두 패널 축 범위를 통일. 측정 당시 렌더러 소스와 후속 렌더러 해시를 보존해 계산 변경과 구분했다.
- 결과: `results/pandas-duckdb/run-20260907T022808832856Z/report.md`, `summary.csv`,
  `measurements.csv`, `memory.csv`, `qa.json` 및 실행·입력 메타데이터.
